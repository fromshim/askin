node_modules/
scan-rs/target/
*.ndjson

# electron-builder 산출물. 로컬에서만 만든다.
dist/

# 화면 확인용 스크린샷. 작업 중에만 쓰고 커밋하지 않는다.
.playwright-mcp/

# 세션마다 바뀐다. 커밋하지 않는다.
.claude/handoff.md
.claude/handoff.md.done
.claude/handoff.tok

# 내부 작업 기록. 공개 저장소에는 안 올린다. 로컬에는 그대로 있다.
docs/_inbox/

# 랜딩 작업 산출물(로고 후보·리뷰 스크린샷). 8.7MB 라 저장소에 안 담는다.
site/.impeccable/
