# askin 작업 규칙

하네스를 점검하는 도구다. 그래서 **자기 숫자가 틀리면 존재 이유가 없다.**
쓰는 법은 `README.md` 에 있다. 결정과 측정 함정의 기록은 저장소 밖(`docs/_inbox/`)에 둔다.
여기는 코드를 고칠 때마다 필요한 것만 둔다.

제품이 무엇이고 누구를 위한 것인지는 `desktop/design-concept.md` 에 있다. **랜딩페이지·소개 글처럼
제품을 밖에 말하는 일은 `site/BRIEF.md` 부터 읽는다.** 무엇을 말하면 거짓말이 되는지가 거기 있다.

## 재는 규율

- **앱 값과 직접 센 값을 대조한다.** 스크립트를 따로 써서 같은 것을 세고 숫자가 맞는지 본다.
  안 맞으면 어느 파일이 빠졌는지 목록으로 좁힌다
- **살아 있는 데이터는 순서를 뒤집어 한 번 더 잰다.** 전사는 지금도 자란다.
  앱을 먼저 재고 직접 센 것을 나중에 재면 나중 것이 크다. 4건 차이를 결함으로 착각한 전례가 있다
- **캐시 등가 검사는 네 번 연속 부른다.** 캐시 없이 한 번, 캐시로 세 번. 값이 같아야 한다.
  **빈 것끼리 비교하면 무엇이든 같다.** 분모가 0 이 아닌지 먼저 확인한다
- **집계 로직을 고쳤으면 `--no-cache` 로 재고, 캐시를 켠 값과도 대조한다**

## 판정의 결

- **축이 판정을 독점한다.** `src/scan.mjs` 와 `src/refs.mjs` 는 사실만 모으고 비율을 계산하지 않는다
- **분모가 비면 예외 없이 판정 불가다.** 0% 도 아니고 "해당 없음"도 아니다.
  무엇이 없어서 못 재는지 단위 이름으로 말한다
- **종합 점수도, 프로젝트를 가로지르는 평균도 만들지 않는다.** 평균을 고르는 것 자체가
  종합 점수를 만드는 일이다. 대신 몰린 곳을 `6/40` 처럼 분모와 함께 보여준다
- **관찰값은 누적 건수면 안 된다.** 건수는 창 크기만 말해준다. 비율이나 100턴당으로 낸다
- **축마다 자기 시간 축과 범위가 있다.** 하네스 활용은 전체 기간을 보고, 전역 정의는
  전 프로젝트에서 센다. 저장소 범위로 좁히면 멀쩡한 정의를 지우라고 말하게 된다

## 범위를 먼저 가른다

전역과 저장소를 섞으면 리포트가 거짓말한다. 이 함정을 여섯 번 밟았다.

- 판정할 때: 전역 정의는 전 프로젝트 기록으로, 저장소 정의는 이 저장소 기록으로 잰다
- 표시할 때: 어디 것인지 말한다. 전역에서 깨진 심링크가 저장소 12곳 전부에서 보고됐다
- 지시서에서: 저장소 밖 파일을 고치는 갈래는 그렇게 말한다. 저장소별로 따로 고치려 든다

## 고치기 전에 묻는다

- **없는 문제에 기계를 붙이지 않는다.** 실제 데이터에서 관측되지 않은 문제를 막으려다
  있는 후보를 버린 적이 세 번 있다. 규칙을 넣기 전에 "이게 진짜를 버리는가"를 실측한다
- **문자열만 보고 못 가르는 것이 있다.** 슬래시로 묶은 클래스 나열과 확장자 없는
  디렉터리 경로는 토큰만 보면 같다. 판정을 정확하게 만드는 대신
  **틀릴 수 있음을 지시서가 말한다**
- 남의 저장소 경로를 백틱으로 인용하면 이 앱이 죽은 참조로 잡는다. 이 문서를 쓰면서
  실제로 2건 냈다. 예시는 백틱 없이 쓴다
- **판정이 반쯤 맞으면 대가를 따진다.** 죽은 경로는 절반이 거짓 양성이다.
  지우기 전에 판단하라고 말해야 멀쩡한 문구가 안 지워진다

## 뜻이 바뀌면 견주지 않는다

- **축의 뜻을 바꿨으면 옛 스냅샷과 비교할 수 없다.** `axisFingerprint` 가 재는 방식의
  지문을 낸다. `label`·`note` 는 표시용이라 지문에 안 들어간다
- **뽑는 함수를 고쳤으면 옛 캐시를 못 쓴다.** `src/filecache.mjs` 가 추출 함수 지문을
  같이 저장한다. 안 그러면 고친 로직을 캐시가 덮는다
- 둘 다 주석과 공백을 뗀다. 안 떼면 주석 한 줄에 비교나 캐시를 통째로 잃는다

## 같은 판정을 두 곳에 두지 않는다

세 번 같은 모양으로 데였다. 한쪽만 고치면 화면과 알림이 다른 말을 한다.

| 판정 | 아는 곳 | 모르던 곳 |
|---|---|---|
| 반올림 흔들림 | `worseThings` | 스냅샷 저장 판정 |
| 축 지문 | `delta` | `worseThings` |
| 스킬·에이전트 scope | `skillIndex` 가 알 수 있었다 | 안 남겨서 아무도 몰랐다 |

판정을 함수 하나로 두고 둘이 같이 쓴다. `comparable()`·`saveKey()` 가 그렇게 나왔다.
**"이 사실을 아는 다른 곳이 있나"를 먼저 묻는다.**

## 검사

- `npm test` 없이 "완료"라고 말하지 않는다
- **비트리비얼한 판정에는 검사를 남긴다.** 실측으로 확인한 수치는 주석에 근거를 적고
  `test/` 에 고정한다. 측정 함정 여덟 개가 그렇게 지켜지고 있다
- 테스트는 사용자 데이터를 건드리지 않는다. `npm test` 가 `HARNESS_BRO_CACHE` 로 캐시를
  격리한다. 캐시 파일 174개 중 171개가 테스트 잔재였던 전례가 있다

## 커밋

- 결이 다른 것끼리만 나눈다. 사후 squash 가 필요 없게
- 커밋 메시지에 **실측 수치와 전후 값**을 적는다. 왜 고쳤는지가 숫자로 남아야 한다
- 내가 바꾼 파일만 명시 경로로 스테이징한다
